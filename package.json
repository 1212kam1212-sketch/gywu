{
  "name": "forged",
  "version": "2.0.0",
  "description": "FORGED - personal workout tracker, static PWA (IndexedDB, no server)",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "node --test tests/",
    "serve": "node scripts/dev-server.js"
  }
}
